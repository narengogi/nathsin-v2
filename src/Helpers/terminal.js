import { createElement } from 'react';
import '../Components/Common/common.css';

export function typer(message: string) {}

// export function element(type, text) {
//   switch (type) {
//     case prompt:
//       return createElement('span', { className: 'prompt', onLoad: typer(text) }, `${text}~>  `);
//     default:
//       break;
//   }
// }
