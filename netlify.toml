[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"
  # create-react-app treats lint warnings as errors when CI is true.
  CI = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
